[package]
name = "mailang-analyzer"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Semantic analyzer for MaìLang"

[dependencies]
mailang-ast = { path = "../mailang-ast", version = "0.2.5" }
thiserror = { workspace = true }

[features]
default = []
