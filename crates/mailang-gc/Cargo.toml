[package]
name = "mailang-gc"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Garbage collector for MaìLang"

[dependencies]
mailang-bytecode = { path = "../mailang-bytecode", version = "0.3.0" }
serde = { workspace = true, optional = true }

[features]
default = ["gc"]
gc = []
alloc = []
