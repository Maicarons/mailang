[package]
name = "mailang-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "C FFI for MaìLang"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
mailang-core = { path = "../mailang-core", version = "0.2.6" }
mailang-bytecode = { path = "../mailang-bytecode", version = "0.2.6" }
mailang-stdlib = { path = "../mailang-stdlib", version = "0.2.6" }
libc = "0.2"
