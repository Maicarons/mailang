# fs - 文件系统操作模块

版本: 0.1.0

## 功能说明

提供文件系统操作功能。

## API 列表

### exists(path)

检查路径是否存在。

- 参数: `path` - 字符串
- 返回值: 布尔

### read(path)

读取文件内容。

- 参数: `path` - 字符串
- 返回值: 字符串

### write(path, content)

写入文件内容。

- 参数: `path` - 字符串, `content` - 字符串
- 返回值: 布尔

### read_lines(path)

读取文件行。

- 参数: `path` - 字符串
- 返回值: 数组

### write_lines(path, lines)

写入多行。

- 参数: `path` - 字符串, `lines` - 数组
- 返回值: 布尔

### append(path, content)

追加内容到文件。

- 参数: `path` - 字符串, `content` - 字符串
- 返回值: 布尔

### prepend(path, content)

在文件开头添加内容。

- 参数: `path` - 字符串, `content` - 字符串
- 返回值: 布尔

### size(path)

获取文件大小（字符数）。

- 参数: `path` - 字符串
- 返回值: 整数

### copy(src, dst)

复制文件。

- 参数: `src` - 源路径, `dst` - 目标路径
- 返回值: 布尔

### read_json(path)

读取 JSON 文件。

- 参数: `path` - 字符串
- 返回值: 对象

### write_json(path, value)

写入 JSON 文件。

- 参数: `path` - 字符串, `value` - 任意值
- 返回值: 布尔

## 使用示例

```mai
import "fs"

// 写入文件
fs.write("test.txt", "Hello, World!")

// 读取文件
let content = fs.read("test.txt")
println("内容: {content}")

// 追加内容
fs.append("test.txt", "\nNew line")

// 检查文件
if fs.exists("test.txt") {
    println("文件大小: {fs.size("test.txt")}")
}
```
