# io - 输入输出模块

版本: 0.1.0

## 功能说明

提供文件读写和用户输入功能。

## API 列表

### input(prompt)

读取用户输入。

- 参数: `prompt` - 字符串，提示信息
- 返回值: 字符串

### read_file(path)

读取文件内容。

- 参数: `path` - 字符串，文件路径
- 返回值: 字符串

### write_file(path, content)

写入文件内容。

- 参数: `path` - 字符串，文件路径, `content` - 字符串，内容
- 返回值: 布尔

### file_exists(path)

检查文件是否存在。

- 参数: `path` - 字符串，文件路径
- 返回值: 布尔

### read_lines(path)

读取文件行。

- 参数: `path` - 字符串，文件路径
- 返回值: 数组

### write_lines(path, lines)

写入多行。

- 参数: `path` - 字符串，文件路径, `lines` - 数组，行内容
- 返回值: 布尔

## 使用示例

```mai
import "io"

// 写入文件
io.write_file("test.txt", "Hello, World!")

// 读取文件
let content = io.read_file("test.txt")
println("内容: {content}")

// 检查文件
if io.file_exists("test.txt") {
    println("文件存在")
}
```
