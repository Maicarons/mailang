# math - 数学函数模块

版本: 0.1.0

## 功能说明

提供数学常量和常用数学函数。

## API 列表

### 常量

- `PI` - 圆周率 π (3.141592653589793)
- `E` - 自然常数 e (2.718281828459045)

### pow(base, exp)

计算 base 的 exp 次方。

- 参数: `base` - 数字, `exp` - 整数
- 返回值: 数字

### clamp(x, min_val, max_val)

将 x 限制在 [min_val, max_val] 范围内。

- 参数: `x` - 数字, `min_val` - 数字, `max_val` - 数字
- 返回值: 数字

### lerp(a, b, t)

线性插值：a + (b - a) * t。

- 参数: `a` - 数字, `b` - 数字, `t` - 数字 (0-1)
- 返回值: 数字

### deg_to_rad(degrees)

角度转弧度。

- 参数: `degrees` - 数字
- 返回值: 数字

### rad_to_deg(radians)

弧度转角度。

- 参数: `radians` - 数字
- 返回值: 数字

### is_even(n)

判断是否为偶数。

- 参数: `n` - 整数
- 返回值: 布尔

### is_odd(n)

判断是否为奇数。

- 参数: `n` - 整数
- 返回值: 布尔

### is_positive(n)

判断是否为正数。

- 参数: `n` - 数字
- 返回值: 布尔

### is_negative(n)

判断是否为负数。

- 参数: `n` - 数字
- 返回值: 布尔

### is_zero(n)

判断是否为零。

- 参数: `n` - 数字
- 返回值: 布尔

## 使用示例

```mai
import "math"

println("PI = {math.PI}")
println("2^10 = {math.pow(2, 10)}")
println("45° = {math.deg_to_rad(45)} rad")
println("clamp(15, 0, 10) = {math.clamp(15, 0, 10)}")
```
