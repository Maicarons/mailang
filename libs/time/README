# time - 时间和日期模块

版本: 0.2.0

## 功能说明

提供时间和日期相关的功能，使用系统原生调用获取准确时间。支持获取当前时间戳、日期、时间，以及时间计算和格式化。

## API 列表

### now()

获取当前时间戳（毫秒，Unix epoch）。

- 参数: 无
- 返回值: Int - 毫秒时间戳

### now_secs()

获取当前时间戳（秒，Unix epoch）。

- 参数: 无
- 返回值: Int - 秒时间戳

### elapsed(start)

计算从 start 到现在的经过时间。

- 参数: `start` - Int，起始时间戳（毫秒）
- 返回值: Int - 经过的毫秒数

### sleep(ms)

休眠指定毫秒数。

- 参数: `ms` - Int，休眠毫秒数
- 返回值: Null

### format_time(ms)

将毫秒格式化为人类可读字符串。

- 参数: `ms` - Int，毫秒数
- 返回值: Str - 格式化后的时间字符串

### date()

获取当前日期字符串（YYYY-MM-DD）。

- 参数: 无
- 返回值: Str - 日期字符串

### datetime()

获取当前日期时间字符串（YYYY-MM-DD HH:MM:SS）。

- 参数: 无
- 返回值: Str - 日期时间字符串

### year()

获取当前年份（UTC）。

- 参数: 无
- 返回值: Int - 年份

### month()

获取当前月份 1-12（UTC）。

- 参数: 无
- 返回值: Int - 月份

### day()

获取当前日期 1-31（UTC）。

- 参数: 无
- 返回值: Int - 日期

### hour()

获取当前小时 0-23（UTC）。

- 参数: 无
- 返回值: Int - 小时

### minute()

获取当前分钟 0-59（UTC）。

- 参数: 无
- 返回值: Int - 分钟

### second()

获取当前秒数 0-59（UTC）。

- 参数: 无
- 返回值: Int - 秒数

## 使用示例

```mai
import "time"

// 获取当前时间
let ts = time.now()
println("当前时间戳: {ts}")

// 获取日期时间
println("当前日期: {time.date()}")
println("当前时间: {time.datetime()}")

// 计时
let start = time.now()
// ... 执行某些操作 ...
time.sleep(100)
let elapsed = time.elapsed(start)
println("耗时: {elapsed} ms")

// 格式化时间
println(time.format_time(3661000))  // 输出: 1h 1m 1s
```
